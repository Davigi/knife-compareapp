[build]
  command   = "npm run build"
  publish   = "dist"
  functions = "netlify/functions"

# Shopify requests now go directly to /.netlify/functions/shopify-proxy
# No redirect needed — the app calls the function URL directly.
